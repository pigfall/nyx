package server

import(
		"context"
		water_wrap "github.com/pigfall/tzzGoUtil/net/water_tun_rap"
)


// serve once
// ready tun interface
// ready connection
func Serve(ctx context.Context)error{
	// { ready tun interface
	// }
}
